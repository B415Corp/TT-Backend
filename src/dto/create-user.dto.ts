export class CreateUserDto {
  readonly name: string;
  readonly email: string;
  readonly password: string;
  // readonly group_ids: string[];
  // readonly role_id: string;
  // readonly subscription_level_id: string;
}
